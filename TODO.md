## Stuff for the future

### Splitting transactions
* Create new transactions for the split amounts
* Link them to matching splits with an id in the tag
    * also prefix the summary text
    * Splitting an already split transaction reenters the split dialog
    * unsplitting deletes extra transactions
    * split parts say "Split from id=34234243"
    * right-click "fix" on amount adjusts the touched amount to make the sum correct
